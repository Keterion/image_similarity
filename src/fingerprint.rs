pub fn gen_fingerprint(pixels: Vec<[u8; 3]>) -> Vec<[u8; 3]> {
    let mut print: Vec<[u8; 3]> = Vec::new();
    let mut sample_set: Vec<[u32; 3]> = Vec::new();
    let (mut r, mut g, mut b): (u32, u32, u32) = (0, 0, 0);
    let mut count = 0;
    
    todo!()
}
